#include <bits/stdc++.h>
int a, b, c, d;
bool count(int a, int b, int c, int d)
{
    if()   
}
void solve()
{
    count(a,b,c,d);
    co
}
int main()
{
    while(scanf("%d %d %d %d", &a, &b, &c, &d) && a == 0)
        solve();
    return 0;
}