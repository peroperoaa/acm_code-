#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main()
{
    int n, m, q; cin >> n >> m >> q;
    while(q--)
    {
        int op; cin >> op;
        if(op == 1)
        {

        }
        else
        +{

        }
    }
    return 0;
}   